CREATE TABLE Contact (
    id int NOT NULL,
    uid varchar(255),
   FIRSTNAME varchar(255),
   LASTNAME varchar(255),
   EMAIL varchar(255),
   PHONE varchar(255),
    MOBILE varchar(255),
    ADDRESS varchar(255),
    
    PRIMARY KEY (id)
   
);